<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    body{
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            background-color: darkseagreen;
        }
     nav{
            color: aliceblue;
            display: flex;
            justify-content: flex-end;
            flex-direction: row;
            background-color: rgb(66, 66, 66);
            padding-bottom: -25px;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        #username{
            color: aliceblue;
            margin-right: 50px;
        }
        .lobtn{
            color: aliceblue;
            font-size: large;
            background-color: rgb(66, 66, 66);
            border: none;
            margin-bottom: 10px;
            margin-right: 12px;
        }
        li{
            list-style: none;
            padding: 2px;

            font-size: larger;
            background-color: black;
            color: aliceblue;
            margin: 0 0 10px 0;
            border-radius: 7px;
        }
        #btn{
            background-color: rgba(0, 0, 0, 0);
            color: aliceblue;
            border-radius: 10px;
            font-size: large;
            border: none;
            padding: 7px;
        }
        #btn:hover{
            background-color: rgba(240, 248, 255, 0);
            color: black;
        }
        li:hover{
            background-color: rgba(240, 248, 255, 0);
        }
        #head{
            margin-right: 75%;
        }
        .container{
            display: flex;
            justify-content: flex-start;
            align-items: flex-start;
            flex-direction: column;
            margin-top: 7%;
            background-color: rgba(229, 226, 226, 0.548);
            margin-left: 26%;
            margin-right: 24%;
            padding: 30px 0 30px 20px;
            border-radius: 20px;
        }
        #lbtn{
            font-size: smaller;
            font-size: medium;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            text-decoration: none;
            color: aliceblue;
        }
        .bt{
            margin-right: -200%;
        }
</style>
<body>
    <nav>
        <h1 id="head">Basic Portal</h1>
        <h3 id="username">Welcome,<%=user.username %></h3>
        <button class="lobtn"><a href="/logout" id="lbtn">Logout</a></button>
    </nav>
    <div class="container">
        <h2>Subjects of the curricullum</h2>
        <% if(data.length > 0) { %>
            <form action="/subject1/data" method="POST">
                <% for(var i=0;i<data.length;i++) {%>
                    <div class="bt">
                        <li><button id="btn" type="submit" name="response" value="<%=data[i].subject%>"><%=data[i].name%> (<%=data[i].subject%>)</button></li>
                    </div>
                <% } %>
            </form> 
        <% } %>
    </div>  
</body>
</html>